# Actual guarded-call prefix review

Final review: clear; chronological investigation follows below.

2026-09-23. Read-only review of the candidate in
`/Users/qobilidop/my/work/p4blo-guarded-call-prefix` against `21410b6`, following
the committed call-body-prefix plan and reviewed two-local initializer choice.
No candidate edits or rebuilds by reviewer. Stable read-only checks were
coordinated with the implementer; mutation experiments are isolated elsewhere.

## Proof boundary and exact composition

The source theorem uses the actual body-bearing WithBody.index and call
entry. Kernel reflexive equations connect the initializer pair to the selected
wrapper and the complete observer-suffix body to CallBodyEntry.body. One real
runBlock transition, the reviewed four-transition initializer prefix and the
same concrete guarded command's steps_prefix compose without a second AST,
binder or executor. Full source meaning is rewritten through the independent
guarded Snapshot policy. Modes, nominal agreement, actual initial frame and
local initialization facts are discharged, not callbacks assumed from users.

Arbitrary source hdr/meta/route, observer Value, caller Run, suffix and K
remain quantified; actual caller lookups/index equality and absent caller
action layers remain explicit. The endpoint is exactly statements suffix,
original-caller blockReturn and K, with no fault. The current callee Run is
the original caller Run with its entire frame replaced. This correctly avoids
claiming ChangesOnlyVars between differing caller/callee scopes. Observer
value and unrelated165 are separately preserved; modeled scratch19 follows
the full source-policy result. Actual scoped initializer/body typing is
separate from runtime existence checks; suffix typing/execution is not assumed.

No whole call, observer behavior, return/copyback or fault-unwind conclusion
is smuggled into this prefix result. In particular invalid-source rejection
changes only drop in the body policy, not in the entire call prefix that
also enters a new frame and initializes locals.

## Independent initial checks and exact counts

Independently ran the initial focused Python suite: **74 passed**, exit 0
(18.46 s). Fresh pinned queries confirmed the two reflexive identity roots
are axiom-free and three typing/source/concrete roots use exactly
`[propext, Classical.choice, Quot.sound]`. Fresh native evaluation passed
192 cases across all validity/hit/TTL/prior-drop combinations and three
suffix forms.

The literal branch counts are independently consistent with the actual
machine: entry/control plus initializer pair contribute six transitions;
each conditional level contributes three administrative transitions; the
reject write costs two. This gives 11/14/17/20/23, with the five-write
rewrite costing eight more than rejection for 31. Tests check exact endpoint
queue, local boundaries after one/six steps, and suffix/continuation effects
if advanced. The native cost table is not a new executable semantics.

Python hooks actual execute_one at the unique first observer statement. The
callback checks values and captured caller before raising PrefixStopped;
the actual call_block finally still copies back afterward. These assertions
are correctly inside the callback, not post-unwind assertions mislabeled
prefix state. The deliberate premature-observer control changes its stored
output and fails. Source/profile expectations are independently constructed
literal finite answers, not the Lean command denotation.

## Confirmed observer gap and acceptance refinement

An independent process-local probe delegated to actual Env.enter_block and
then changed packet.cursor from integer 3 to float 3.0. The complete prefix
observer for guard-true-true-true-2 passed, with the injected hook hit once.
The cause is shared_snapshot(callee) == frozen_shared: Python recursive
equality conflates numeric types even though value-map comparisons use strict
JSON. Requested a type-sensitive full frozen shared-state comparison and
permanent negative controls. This is a real observation survivor, not a
failure of the Lean theorem.

Initial native assertions/projected snapshots cover selected sentinels and
index.program rather than every Index/scope map; installed entry arrays are
empty. The note honestly limits that evidence and reserves full preservation
for the theorem. Nevertheless the staged acceptance requested complete
native maps/scopes/shared contents. Root chose to strengthen those observations
and add nonempty entries now, rather than repeatedly defer that obligation.
Final review must check the extended projection, its independent answers and
the corresponding strict/type and state-corruption regressions.

## Corrected observation boundary and independent recheck

The local strict shared comparator now checks exact scalar/container types
recursively through the immutable tuple representation, including bytes.
Permanent actual enter_block delegation tests explicitly demonstrate that
the old comparator survives cursor3-to-3.0, sentinel visit1-to-True and
bytes-to-bytearray, while the new prefix callback rejects each. A newly
nonempty installed-entry array makes clearing entries observable. Independently
reran the exact earlier cursor probe: it now fails specifically at prefix
shared state after one actual injected entry, as intended.

Native sameMap compares size and every lookup, without depending on iteration
order. sameScope covers the block and every declaration/action/table/state
map; sameIndex covers program, all type/instance/block/scope maps, program
names and errors. sameShared includes the installed Index, entries/defaults,
all extern instances, packet, emitter and visits. Actual caller/callee scopes
are checked by these complete comparators; frame values/action fields retain
their existing independent checks. Constructor-by-constructor ExternState
comparison covers all current variants. Corruption controls exercise every
Index/scope component and shared-state groups, including installed index.
The exported JSON remains the smaller documented boundary projection; it is
the native suite, not the wire export, that gained complete map comparisons.

Independent final checks:

- Focused Python file: **78 passed**, exit 0 (19.09 s), including all four
  new shared-state controls and the saved/local-write replay regression.
- Fresh five-root audits retain exactly two axiom-free/three standard-three
  sets; fresh 192-case native run and full compiled userTests pass.
- Additional fresh native checks show sameMap accepts reversed insertion
  order and rejects a changed value with the same keys/count.
- Actual saved bundle is 50175 bytes, SHA256
  `3ef594c90fab7df89131d2983c3bab3cfe8004dbc22dfdf0c3017182376d6fe3`.
  Independently matched its complete program to the current guarded exporter
  and tracked guard-false-false-true-2 wrapper, one empty-entry ingress-0
  deadbeef request, four ports and seed zero. Restored replay agrees once
  without errors. This is the same input already retained by the earlier
  guarded-forwarding campaign, not an additional distinct witness.
- Fresh guardedCallPrefix stdout is 866173 bytes and retains SHA256
  `eb3f2ef539102c316ec8b3c63903d998d06c0fb7e26c45bff008b81140229327`;
  no exporter-wire change accompanied the stronger native checks.
- Whitespace check and production IR/Python source-diff checks pass.

Final source-mutation/restoration evidence and completed note remain to be
reviewed before final clearance; no outstanding core proof finding.

## Final mutation and restoration review

Inspected the completed note and actual retained logs. Removing the pending
statements suffix from the theorem's public endpoint fails on the real
incompatible trace endpoint: a genuine proof rejection, not an execution
mismatch. Changing the independent invalid-Ethernet count from eleven to
thirteen passes default/audit and native-executable builds, then native
execution fails after advancing into return and the Python independent count
answer rejects thirteen. This is correctly classified as a compiled test
fixture observation fault, not a production IR mutation.

The separate actual Python assign-source mutation changes only local
scratch8/19 to 20. Its prefix execution fails inside the pre-observer callback;
the packet profile separately saves one genuine output mismatch with no
errors/diagnostics/state effects. Live CLI replay diverges only at byte40;
restored CLI replay agrees. The note explicitly distinguishes direct prefix
inputs from the packet wrapper's different initial values and does not claim
the packet replay proves an internal queue boundary. Earlier selected fault
logs and the final restored 78-test gate are separate checkpoints; no claim
that all of their collection counts were identical is needed.

Independently compared all four final candidate/restored source files
(core theorem, native tests, Python test, production stmt.py): each cmp exits
0 and every SHA256 matches the note. The restored mutant worktree has empty
production IR/Python diffs. Executed both candidate and restored exporters:
guardedCallPrefix, callEntry and callBodyEntry all have identical complete
stdout, empty stderr, successful exit codes and the three documented sizes/
hashes. Inspected successful restored default/native and 78-case focused logs.
Owner's final required suite is recorded as 673 passed, 1475 deselected, no
skips; two-package/static/required gates remain attributed owner evidence.

No remaining blocking finding. Clear for the exact guarded call prefix and
its strengthened observations. No source/runtime change was made by the
reviewer. Actual observer execution, normal return composition and whole-call
or full packet correctness remain outside this checkpoint; merged full gates
are the integrator's responsibility.
