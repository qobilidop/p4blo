# Actual plain-root call-entry review

Final review: clear; chronological investigation follows below.

2026-09-23. Independent read-only review of all candidate changes/new files
in `/Users/qobilidop/my/work/p4blo-plain-call-entry` against `ad746c5`. Read
the integrator's earlier same-path preliminary review before this pass.
Candidate source changes remain owned by the implementer; reviewer was given
exclusive build access for the independent gate.

## Exact theorem boundary

The raw theorem unfolds actual dispatch, exact lookup/arity, real forBlock
and its four-pair argument loop. Its result records the ordered inserts,
exact whole Run frame replacement, original caller captured in blockReturn,
and pending runBlock followed by return and arbitrary continuation. No
alternate runtime binder or assumed whole-call correctness callback appears.
Actual input/inout lemmas read caller roots without changing Run; out ignores
even an unreadable argument and uses the real type initializer. Unknown block
and wrong arity have separate unchanged-Run error laws.

The user theorem derives initialization from the reviewed source/frame laws.
Arbitrary typed hdr/meta/route source data enter the callee, while scratch
remains zero. Observer is preserved as an arbitrary operational Value; no
implicit observer typing/global argument-validity claim is made. Unrelated
is also zero. Exact scope and absent action layers are established; the
bounded caller BlockFrame premise is explicit even though raw read lemmas
can be stronger. Caller construction and later local assignments are absent.

The Index.build witness really builds the selected RewriteBody declaration
program, including H/Result and all extras. Its empty body is intentional
because the theorem stops before runBlock. It does not establish the full
Python wrapper or an authored body under that index. The independent selected
declaration/type/argument comparison preserves nominal field order and rejects
duplicate selected declarations before dictionary comparison.

The bad extra counterexample proves actual Index.build can succeed while the
required extra zeroability is impossible; native checks separately exercise
the failed actual initialization. The documented abandoned whole-loop kernel
reduction is not misreported as a proof. Concrete nonempty caller witnesses
and default audits cover application and negative roots, not only generic laws.

## Observer review and independently reproduced gap

The integrator's earlier aliasing finding is addressed: Python compares
shared identities and immutable content snapshots, freezes caller values,
deep-copies the complete Index and original caller scope/action layers, and
compares selected callee scope. It includes a nonempty extern register.
Eight permanent weak-survivor/strong-reject controls exercise cursor, caller
storage, extern, visits, entries, emitter, index lookup maps and caller action.
Snapshots occur inside intercepted run_block after the real call_block binder,
before its later copyback. This is an entry observer, not whole-program DRT.

This review found an additional type-confusion survivor. On the actual exported
false/false snapshot, replacing `vars["hdr"][2][0][2]` (stored Ethernet
validity) with integer 0 and faultNone with integer 1 still passed the entire
entry test. Python's ordinary equality conflates those with false/true.
The candidate's semantic theorem is unaffected, but its independent JSON
observer falsely accepts malformed evidence. Requested type-sensitive whole
snapshot equality and exact four unique Boolean validity-pair validation,
with retained malformed-export negatives. Root authorized the implementer's
tests-only fix; final recheck is recorded below when complete.

## Independently executed checks before that tests-only fix

- Absolute pinned `scripts/check-lean.sh`: exit 0, both packages, default
  audits, 488 specification checks and all user suites. This is an independent
  gate using the existing caches, not a claim of an uncached reconstruction.
- Focused Python suite: 15 passed, exit 0, including the eight actual-entry
  mutation regressions.
- Fresh pinned compiled audit queries for all twelve newly advertised roots:
  exactly `[propext, Classical.choice, Quot.sound]`, no unexpected axioms.
- Restored Exec.lean, stmt.py and declaration fixture SHA256 hashes match
  the note; production Exec/stmt source have no surviving mutation.
- Whitespace check passes; public exports/default exporter/test-driver/audit
  registrations reviewed.

No production proof/implementation finding. The known JSON observer survivor
must be rejected before final clearance. Complete combined integration gates
remain the parent's responsibility.

## Final correction and clearance

The tests-only correction closes the reproduced survivor. Native observation
now compares the complete snapshot against an independently constructed JSON
object through the existing type-sensitive canonical-render helper. Before
selection it requires exactly four records, strictly Boolean case flags, and
the complete set of four distinct validity pairs. Seven permanent negatives
reject integer stored validity, integer fault flags, Boolean zero-valued bits,
integer case flags, duplicated pairs, missing cases and extra cases.

The final Python observed list uses the same type-sensitive comparison. Its
additional permanent actual argument-value fault changes only the copied
Ethernet validity from false to integer zero. The caller remains unchanged;
the test explicitly proves ordinary equality survives and the corrected
output observer rejects. This avoids relying on caller-state checks to detect
the malformed callee result accidentally.

Independently reran the complete final focused file: **23 passed**, exit 0
(0.20 s), including all eight new type/case controls and the eight earlier
weak-survivor/strong-reject state controls. No Lean rebuild was necessary or
performed after this tests-only change. Whitespace check still passes and
the actual Exec.lean/stmt.py diffs remain empty.

Inspected the retained five successful actual Exec mutation-build logs and
their corresponding failed proof logs. Wrong destinations, missing observer,
wrong captured caller and changed packet leave genuine false correspondence
goals. The skipped-observer log also has an unused-simp warning, but already
contains the substantive missing-insert equality failure; it is not counted
as a warning-only kill. The out mutation breaks the actual argument-value
correspondence. These remain proof rejections, not executed runtime tests.

Separately inspected the paired fixture's successful proof/audit/exporter
build and independent declaration-test rejection, plus the actual Python
cursor fault's four executed state mismatches and restored 13-test pass at
that earlier checkpoint. Those are correctly distinguished from proof kills
and from packet-program DRT. The final implementer required-gate log reports
569 passed, 1442 deselected, no skips (49.54 s); this is attributed evidence,
not an independent second full differential run.

No remaining blocking finding. Clear for the scoped plain-call-entry proof
and tests, subject to the integrator's usual combined gates. The empty-body
declaration witness, manually constructed caller, pre-body observation,
unproved later copyback and absence of a whole-wrapper theorem remain
explicit limits; this clearance does not remove them.
