# Actual forwarder table application review

Final review: CLEAR for this bounded actual table-application checkpoint.
Chronological structural review and final independent evidence follow.

2026-09-23. Read complete `P4blo/ForwarderApply.lean` in the isolated
`p4blo-forwarder-apply` tree at `b3635a0`, against the accepted table plan and
committed selection/action boundaries. No candidate builds or consumers run.

## Actual selection and action composition

The independent effect policy composes the numeric/equality selection policy
with the already independent full-field forward-action policy, literal drop
update or identity. Defaults remain misses even when they forward. Actual
application does not invent an IPv4/Ethernet-validity guard or clear a prior
drop during forwarding. The surrounding control is intentionally outside scope.

`dispatch_table` resolves the actual scope/table, reads the real nested key
through the existing field theorem, and uses the actual installed family and
proved lookup result. Its conclusion retains the selected action followed by
the actual `writeHit` administrative work, leaving the entire Run unchanged
at that boundary. Index, scope, exact installed value and frame agreement are
explicit premises, not supplied by a successful-lookup callback.

Forward action execution reuses the committed real action-layer proof. Drop
and NoAction are separately derived from actual tableAction dispatch, actual
action frame entry, statement execution and normal actionReturn. Drop's write
uses the unshadowed block-root law under the concretely empty action map.
NoAction restores even an arbitrary original action layer; the combined public
application theorem explicitly requires BlockFrame for the branches that need
it. There is no substitution of a block-only write theorem inside an unchecked
active action layer.

## Queue and state boundary

`before_hit` leaves the exact supplied optional target and hit value pending
ahead of arbitrary continuation work. It does not claim that arbitrary target
writing succeeds or is well-typed. The completed `source_steps` and `run_correct`
theorems specialize to the actual operation's absent hit target and consume
that real administrative step. Finishes.sound connects the finite actual
machine chain to applyTable; there is no alternate executor.

The result is the complete Run, with exact selected source-root changes and
all other components unchanged. Source policy frame agreement, ChangesOnlyVars
and outside-root preservation are explicit. Actual initialized/populated frame
and actual installed-index construction provide a nonvacuous arbitrary-store
instance. The unindexed Steps relation is not promoted to a numeric step-count
theorem; independently literal 13/7/5 counts remain a test obligation.

## Remaining acceptance

Final fixtures should exercise both hit orders/default-forward miss, prior drop,
invalid stored headers and wrapping TTL without silently adopting surrounding
control guards. Independent full state and pending-work snapshots must expose
key-read side effects, action restoration and success-only hit timing. An
optional target case should distinguish executing writeHit before/after the
selected action, while remaining a separately bounded test from the public
absent-target completion theorem. Faulting/nonempty continuations must remain
unexecuted at the promised boundaries. Exact built inputs and all nested
stored fields need independent intent anchors, not only engine agreement.

No core structural finding requires a change at this stage. Whole ingress,
checksum continuation, parsing and final packet behavior remain later work.

## Independent fixture checks

Against the owner's frozen candidate, the reviewer ran the final Python file
excluding its BMv2 case: 319 passed, one deselected, exit 0. No Docker operation
was performed. A fresh pinned Lean query ran the native suite successfully:
270 table applications, 810 exact queue boundaries and 24 hit/error controls.
The same query printed all twelve new public audit roots; each uses exactly
`propext`, `Classical.choice`, and `Quot.sound`.

The full detached entry/action/return snapshots, key and lookup delegating
hooks, optional-hit timing controls, and independent complete packet answers
were inspected. The inventory cycles stored-state profiles across its 270
selection cases; it does not claim the Cartesian product of every profile.
Expected key/result and state construction does not call the production
evaluator or lookup being tested. Pending-work and malformed-action controls
remain separate from the public absent-hit-target theorem. No observer
coupling issue was found in this pass. Owner campaign results and restored
artifact/source identity still need final review.

## Final campaign and restoration closure

Read the complete final note and the actual isolated campaign logs. Removing
writeHit compiled the real Exec module and produced a genuine False queue
goal in dispatch_table. Replacing destination-key evaluation with source-key
evaluation also compiled Exec and left the real semantic goal unsolved; the
additional simp-linter failures are not the detection evidence. Neither is
misrepresented as an executed packet mismatch.

The paired wrong-intent native source fixture built the general proofs and
exporter, then failed the independent complete-before fixture check. The
source-edited Python default-skip fault failed normal internal hook counts and
the packet test; the latter retained its complete input before assertion.
Inspected live/restored logs distinguish a clean output divergence from
errors and show restoration agreement. Reviewer execution of the permanent
live/restored process-local regression was included in the 319 focused checks.

Independently verified all five candidate/restored-clone source pairs, empty
runtime/Forwarder/golden diffs in both trees, and the complete new exporter
capture equality: 49,719,204 bytes, SHA-256
`aab4a8f59ec967c1f499a5f2b85c496370acc656c64f5ed00254f28075daeb97`.
The new packet bundle matches between both worktrees: 26,918 bytes, SHA-256
`39b7ebeeb12760cd89e0b408117d7bf9ba1eb8f8589c7fe3d9baf001a70bcc81`.
The reviewer loaded it, checked the unchanged full golden Program, the tracked
`application_packet("empty/drop/false")` request, ports 4 and seed 0, and replayed
the restored native binary: one agreement, no protocol or both-error outcome.
The packet bytes are reused; the no-route host configuration distinguishes this
request. The owner's broader 19-bundle deduplication inventory was not rerun.

Owner logs, separately attributed rather than rerun here, report final both
Lean packages/default audits, 320 focused including BMv2, 2,124 required Lean
comparisons with no skips, and full 3,900 passed / 5 existing strict xfails /
one unavailable local XDP-image skip. The restored clone's complete 320-case
focused suite also passed. No candidate rebuild or external oracle run was
performed by the reviewer. Main integration still owns combined final gates.

No outstanding finding blocks these scoped commits. Checkpoint 3 remains
the real guarded ingress prefix, with checksum work left pending; this review
does not expand the theorem to that boundary or to a whole pipeline.
