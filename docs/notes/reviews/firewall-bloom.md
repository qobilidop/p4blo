# Actual firewall Bloom insertion review

Final review: clear. The independently confirmed Python survivor below is
fixed; final initialized-witness and campaign checks appear at the end.

2026-09-23. Read-only review of `p4blo-firewall-bloom-next`, following the
accepted insertion plan/probe. No candidate sources changed or builds started.

## Actual execution and scope

The public insertion theorem uses the actual index, action-first reads of the
two 32-bit positions and actual one-bit register states. Private `write_call`
opens actual extern instance/type/method lookup, argument evaluation, register
call and state update. It does not assume an evaluator/call-correctness callback.
Both arguments are in-only and the authored return destination is absent.

The first statement theorem preserves an arbitrary pending work tail and gives
the exact first map insertion. The full insertion theorem explicitly composes
actual statement-list dispatch, both calls and empty-list completion through
`Finishes.sound`. The second register lookup is derived through the first
distinct-name insert; the unchanged frame/index preserve its read premises.
The conclusion is equality of the complete actual execution result and Run,
not merely a helper's cell behavior. Ordered map inserts avoid assuming fresh
maps have identical internal structure.

Pointwise extern lookup includes absent/unrelated keys; exact cell observations,
length, selected-cell establishment and existing-one membership preservation
follow separately. Arbitrary natural cells are allowed only in the low-level
Lean state. Numeric monotonicity is not claimed. The 4096-cell corollary has
explicit size and in-range premises; it does not infer these from bit width or
claim the hash masks/computation have been proved.

Native fixtures cover 84 states, including fourteen native-only noncanonical
natural-cell profiles. Independent enumeration constructs expected whole-array
updates, and full Run comparison observes action overlays, unrelated externs,
frame/maps/shared state. The first actual step checks both complete intermediate
state and exact still-pending faulting sentinel syntax. Python's corresponding
seventy profiles use only accepted one-bit values; shorter arrays are clearly
lower-level operational states, not startup profiles. The first-write call
trace distinguishes reversed execution even when complete final arrays agree.

## Confirmed expectation-coupling survivor

The initial Python `observe_insertion` computed its expected positions by
calling production `env.read`, then actual argument evaluation called that
same method. Independently probed this using a scoped monkeypatch, without any
file edit: alias only `reg_pos_one` to `reg_pos_two`, using the real golden,
shape 3, intended positions 1/2 and an active action overlay.

The complete observer returned successfully. The hook ran twice: once while
constructing the expectation and once in actual execution. The first register
became `[1, 0, 1, 1]`, whereas the independent intended result is
`[1, 1, 0, 1]`. Complete state coverage cannot repair an expected value derived
through the same faulty production lookup.

Reported the concrete survivor to the owner, who accepted the fix: pass intended
positions explicitly from the independently authored profile, do not call
runtime `Env.read` while building expectations, and retain actual wrong-position
and action-shadowing regressions. The repaired observer should reject after
normal actual execution, with the runtime-only hook hit verified. That change
and later campaign/restoration evidence remain to be reviewed before clearance.

## Repaired observer verification

The observer now receives the intended `(p, q)` explicitly. Its expected trace
is computed from independent enumeration and those supplied values before any
production execution; it makes no `Env.read` calls. The retained real lookup
alias regression now observes exactly one runtime hit, normal execution with
the independently wrong first array, and rejection by the complete trace.
Independently ran the final focused file: **79 passed**, exit 0, 5.67 seconds.

A fresh pinned Lean query independently confirmed all twelve audited roots
match their recorded standard foundations: five elementary array/profile laws
use `propext`; the other seven roots use exactly `propext`, `Classical.choice`
and `Quot.sound`. The full native entrypoint passed 84 complete-state and
first-write cases, fourteen explicitly native-only. In particular,
`result_preserves` correctly reports the standard three despite its immediate
proof, because its statement contains the actual map-updating result.

Also independently challenged the trace with a real delegating register-call
fault that corrupts the action sibling after the first call and repairs it
after the second. Both actual calls complete in order and the complete final
Env agrees with the independent expected state, yet the intermediate trace
rejects. This confirms that it observes the first boundary rather than merely
rechecking final cells or final state. No additional observer coupling or
whole-state gap was found in this pass. No candidate file or binary changed.

## Final initialized witness and campaign closure

The added `initializedRun` uses the real previously proved initialFrame.
`initialized_insertion` discharges the actual zero position reads through
`frame_locals_zero` and the two concrete register-map lookups, rather than
assuming their evaluation. Caller-supplied arrays and only the required register
states are correctly not called a complete freshly loaded startup state.
Independently queried the thirteenth audit root: exactly the standard three
axioms, exit 0. The native 84-case suite also independently passed again after
this addition.

Inspected all six actual source compilation/proof logs. First/second target,
position, value, omission and reversal edits each compile the real authored
module, then reject the unchanged literal body/order equation. Secondary
elaboration heartbeat failures in position/value variants are not counted as
additional semantic detections. Reversal concerns the claimed sequence, not
unequal final logical arrays. The separate actual Lean register-zero runtime
compiles, then rejects `write_call` at a genuine whole-Run zero/one discrepancy.
These are proof rejections, not runtime differential kills.

The final actual Python source-edit campaign is documented precisely, including
the initial save-after-assertion run and the subsequent retention-first rerun.
The latter records normal two-write completion, complete observer rejection,
three clean state-only packet-run discrepancies, and identical live replay.
Both logs and final restoration records support those classifications.

Independently checked all nine candidate/fault-tree source pairs, empty diffs
for the actual IR/Python runtime and authored firewall source, and both saved
filenames against the previously retained main firewall input. Its 76260-byte
SHA-256 is
`c547f2999038dae030dfd93402e7e26a73782f0424384fda9633a5ee7e886103`.
The complete loaded Program equals the unchanged golden, the four requests
equal the tracked `connection()` cases, and ports/seed are exactly 4/0. This is
the old input under a new fault, not another independent witness.

Additionally reproduced the operational Python effect with a scoped delegating
real Register.call monkeypatch, without file edits. It runs both actual writes,
is rejected by the full intermediate observer, and yields exactly three clean
state-only discrepancies over the four retained requests with identical packet
outputs and no errors/diagnostics. Removing the patch gives four agreements on
the same saved input. This independently checks observer/replay behavior; it
does not relabel the owner's separate source-edit campaign as reviewer-owned.

Final owner-attributed gates are both Lean/default suites (567 spec checks),
all thirteen audits/native cases, 79 focused/static checks and 1835 required
comparisons. The broad 3608-pass/five-existing-discrepancy/one-local-XDP-skip
gate loaded the earlier 78-test revision before the repair and witness. The
note accurately distinguishes that revision; main integration owns the final
combined full gate. No candidate rebuild or file edit was made by this review.

No remaining blocker for this insertion checkpoint. Actual readback, drop
composition, hash/mask bounds, reverse-flow inputs and positive firewall control
branching remain separate. The preserved collision counterexample still rules
out an exact connection-tracking claim.
