# Source-zero to actual frame review

2026-09-23. Independent read-only review by the integrator of isolated
`work/initial-source-frames`, based on committed main `db99dc2`.
Final review: clear. Combined integration gates remain the integrator's next
checkpoint.

## Scope and premises

`Layout.initialize` composes the actual Frame.forBlock theorem with the
independent structural source-zero theorem. Modeled roots discharge their
actual zero equations from RootDeclares, nominal agreement and sufficient
production fuel. Every remaining actual scope entry needs explicit successful
zeroing. The theorem retains exact scope identity, both absent action fields,
FrameMatches and the lookup equation for every name, not just modeled roots.
It introduces neither a second initializer nor a semantic callback premise.

RootDeclares does not imply full coverage. The separate Covers predicate
quantifies every actual map entry; Modes.scope_covers discharges that condition
only for its own generated scope. Its absence lemma and the negative extra
fixture prevent an unjustified inference that arbitrary scopes have no extras.
The generic theorem does not require global well-formedness; the Modes
corollary uses the existing root well-formedness condition for declarations.

## Constructive and independent evidence

The forwarding Index is computed by actual Index.build, with a named success
equation, not only a getD fallback. Kernel cbv establishes exact scope/mode/map
facts; a separate lookup proof establishes coverage without equating HashMaps
constructed in different insertion orders. Both actual-build and concrete
frame/expected-value theorems are default audit roots. This is explicitly the
declaration program, not the packet wrapper, call entry or body execution.

Expected values use direct IR constructors, independently of both zero
functions. They include every modeled root and both invalid header bits.
Successful extra observer/unrelated variables extend the real-built scope
by hand, explicitly distinguished from an Index.build result. A missing-header
extra has a kernel proof that the extras premise is false. All parameter
directions plus a local have an instantiated Modes theorem. Native checks
also pin storage cardinality, missing names, action absence and exact errors.

## Adversarial evidence inspected

The implementer's false scope.block substitution fails at the exact generic
result; its diagnostic is a logical adapter rejection, not runtime detection.
Changing only the observer declaration from 65 to 64 bits leaves the generic
adapter build successful but rejects the independent expected 65-bit value.
The actual reported compiler goals confirm those failure reasons. Both
restored source hashes match the tracked note. This reviewer inspected the
fault patches/logs and restoration, but did not rerun the live mutations.

No global initialization validity, general fuel adequacy, caller binding,
copyback, parser unwinding or whole-program result follows from this bridge.

## Independent checks

- Pinned build of InitialFrames, InitialFrameTests and UserProofAudit: exit 0.
- User-package lake test: exit 0, including the new full modeled-root/extras,
  invalid-header, failed-extra, fuel and all-direction observations.
- Fresh pinned Lean stdin query: all seven advertised generic/concrete audit
  roots have exactly `[propext, Classical.choice, Quot.sound]`. Evaluating the
  focused native test driver in that query also exits 0. In particular, the
  actual-build and forwarding witnesses contain no native reduction axiom.
- Source SHA256 checks exactly match both tracked restoration values.

The fresh-cache two-package gate (481 spec checks) and 538 required differential
checks without skips are attributed to the implementer, not independent full
reruns. No main binaries were rebuilt while their differential tests ran.
