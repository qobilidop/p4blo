# Real forwarder table-application plan review

Final planning review: CLEAR. This approves the bounded next-step design,
not implementation or completed table/application correctness.

2026-09-23. Read complete plan and unregistered probe in
`p4blo-forwarder-table-next`, actual Tables.lean installation/lookup, Exec table
dispatch/action return, Python stmt.apply and the authored Forwarder definitions.
No candidate edits, builds or running probe were used during this pass.

## Scope and contract

The five installation shapes and three concrete default choices are a useful
small next boundary after the selected-action theorem. All query addresses and
fitting route/default payloads remain quantified; finite native boundary cases
are supporting evidence, not substitutes for those quantifiers. Independent
numeric interval/equality selection avoids defining the policy through lookup,
prefix comparison, authoring accessors or execution.

Actual Index.build, Installed.build and Frame.forBlock witnesses are required.
Both installed and Run indexes and actual scope/table identity matter. A private
lookup decomposition using exact map views is appropriate when the exported
theorem discharges those views from the real successful build; assuming lookup
returns the desired answer would not be. The probe's temporary getD wrapper
is not a vacuity issue when each used concrete build is separately proved ok.

The plan correctly distinguishes host absence restoring the real drop default,
explicit empty NoAction execution, and generic Match.action=none. The last is
not realizable by valid host installation of this unchanged Forwarder; its
operational control must remain separate. Forwarder's drop changes only drop,
unlike the tutorial firewall's additional egress511 write. Prior drop is not
cleared by forwarding, TTL0 wraps, Ethernet validity does not guard selection,
and the pending checksum has not yet changed its stored field.

## Queue and observer checks

Recomputed from actual dispatch: starting at table gives 13 forwarding, seven
drop or five NoAction transitions. Starting at the original conditional list
adds list expansion, condition, branch-list expansion, apply-statement expansion
and branch-list pop: 18/12/10; invalid IPv4 takes three and need not inspect
entries. Optional absent action's two-step case is only a separate generic
control. Unindexed Steps must not be advertised as a numerically indexed law.

The proposed real Python apply observation, exactly one key evaluation, detached
whole-state expectations, intermediate snapshots and pending-K effects address
the meaningful boundaries. The separately labeled hit-target/dropFlag control
makes hit-after-action visible; default forwarding remains a miss. Installer-
rejected failing-action controls cannot serve as successful host witnesses.
Retaining asymmetric data and both route orders is necessary to kill wrong
winner/data errors that packet-only or equal-payload cases can hide.

The bounded checkpoints sensibly separate install/selection, actual application
and first-conditional prefix. No whole ingress/checksum/parser/packet-fate or
arbitrary-prefix/list proof is implied. A small BMv2 packet check can corroborate
the bounded profile; SpecTec's priority-based LPM adapter is not independent
longest-prefix evidence. No plan change requested so far.

## Final feasibility boundary

Owner narrowed the final probe to six concrete actual-build/lookup equations
and the universal shift-to-numeric-range arithmetic lemma. Read the updated
plan: it explicitly records that symbolic full-loop unfolding elaborated but
failed kernel recursion checks at 4096/8192, and a larger-limit exploration was
stopped rather than counted as success. No incomplete theorem remains in the
committed probe. Fixed payload/both-order concrete answers are not silently
promoted to universal five-shape/arbitrary-payload selection evidence.

Independently executed the frozen probe with pinned Lean v4.34.0,
`-DwarningAsError=true`, user library before spec library in LEAN_PATH; exit 0,
3.38 seconds. All seven named roots print precisely propext, Classical.choice,
Quot.sound. No candidate build or source edit was performed. The actual build
equations ensure the probe's getD default is unreachable on both used inputs.

Confidence is medium in the universal loop proof engineering, as now recorded
by the plan. The implementation must preserve its all-address, five-shape,
arbitrary-fitting-data acceptance and discharge private exact map-view/loop
lemmas from real installation; it must not replace that work with a correctness
premise or quietly freeze payloads. First split small actual step equations
from concrete index/install reduction if recursion remains a problem. Larger
kernel limits alone are not the proposed acceptance strategy. Final command,
seven-root result and this independent review should be recorded in the plan
before its documentation-only commit. No further conceptual blocker found.
