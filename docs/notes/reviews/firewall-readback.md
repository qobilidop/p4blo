# Actual firewall two-read prefix review

Draft structural review: core boundary is clear. Preservation corollaries,
constructive initialized witnesses, native/Python observers, audits and actual
fault/restoration evidence remain pending; not final checkpoint clearance.

Read the complete unregistered TutorialFirewallReadback module in the isolated
readback worktree at 25064ab against actual Exec/Externs/Frame and the separately
reviewed readback plan. No candidate builds or consumers were run here.

The `setRoot` result is explicit action-first storage update, not an assumed
successful evaluator callback. `root_write` discharges it from the existing
actual action-hit and unshadowed block-write laws. A successful actual read
premise provides destination existence, with old values unrestricted. The
action-hit branch preserves block decoys; the block branch preserves every
active action binding. The helper also permits names that coincide in a
standalone one-call use: the index input is evaluated before copy-back, so this
does not undermine the concrete distinct-name composition.

`read_call` unfolds the real declared method, out-value zero initialization,
input read, Externs.call/ExternState.call and actual writeLValue. It explicitly
retains register-map reinsertion before the destination update. There is no
direct read of the old out destination merely to form call arguments, no
assumed extern result, no alternate decoder/executor, and no BlockFrame premise
misapplied to active action storage.

`cellValue` uses the actual wrapper and `cell_answer` independently exposes
the modulo-two representation. Its equality prevents a changed wrapping rule
from hiding behind the same operational definition. Future literal tests must
anchor raw 2/3/7 and OOB/empty behavior independently; a paired wrong answer
definition could otherwise still require the usual independent-intent controls.

The concrete first/second calls are tied to checked authored references.
`body_identity` anchors both target/position/result names and the complete
pending conditional, not only a two-call approximation. `root_read_other`
establishes that the first destination write leaves the second position and
destination existence intact even across mixed action/block storage. Distinct
extern names discharge the second register lookup without presuming that a
read produces a structurally identical map.

`first_step` exposes the exact post-copy-back Run with arbitrary following
work. `source_steps` composes four actual next steps and leaves the existing
conditional list plus arbitrary continuation unchanged. It does not evaluate
the condition, enter drop, pop that list or execute the continuation. The full
result retains both ordered map reinsertions and storage updates. No narrower
frame-only equality substitutes for whole Run.

No structural blocker found. Before final clearance, require separate exact
outside-root/layer and pointwise extern preservation, a constructive actual
initial-frame instance, all four destination-layer combinations and independently
shadowed position tests, first-call full-state/order checkpoints, strict detached
Python expected states with supplied intended positions, and the reviewed actual
runtime/observer campaigns. Numeric four-step counts are native evidence unless
the public relation is made indexed. No whole checkBloom/firewall claim yet.
