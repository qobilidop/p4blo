# Tutorial firewall initialization and invalid-body review

Final review: CLEAR. Chronological structural and observer checks follow;
the final section closes actual fault evidence, restoration and gate provenance.

2026-09-23. Read complete TutorialFirewallProof and TutorialFirewallTests in
`p4blo-lean-firewall` after the separate source/execution checkpoint. No source
edits, builds or binary consumers were performed for this early pass.

## Actual initialization and nonvacuity

Index and initialFrame use getD wrappers only alongside kernel equations that
prove actual Index.build and Frame.forBlock succeed. The private initialized
witness reuses Frame.forBlock_initialized rather than copying its semantics.
Zeroability is discharged for the actual scope declarations; aggregate header
zero uses the independent source zero theorem with actual declaration maps and
the concrete fuel bound. No abstract frame-success premise is supplied by users.

The public frame_values equation accounts for all declarations and all absent
names, not just a selected list. Additional explicit independent answers pin
all seven real locals, including bit widths and the false Boolean. Scope and
absence of both action layers are separately stated. The kernel roots-agree
witness uses all nine source roots and actual nominal declarations. It does
not claim whole-program typing or initialized extern state.

## More general body theorem

The main premise is deliberately weaker than initialization: exact global
index plus actual frame.read? hdr returning a headers struct whose IPv4 member
is an invalid ipv4_t header. Ethernet/TCP values and stored IPv4 fields are
arbitrary Values/list, with no unnecessary typing/length restriction. The
index determines the headers.ipv4 position; actual action-first read supplies
the root. Scope, metadata, locals and both action layers remain unconstrained.
No BlockFrame assumption is applied to a frame with an active action overlay.

invalid_guard proves actual evaluation returns false with the same Run. The
body theorem composes actual Execution dispatch/step and Finishes.sound through
both real conditionals and list-pop machinery. Its result is exact successful
execute ingress.body and whole-Run equality, not merely equal output packets or
selected state projections. It does not execute a call wrapper, parser or
positive branch, and no CRC/Bloom/pipeline result follows. The initialized
corollary separately instantiates this broad read theorem with a constructed
actual-initialized frame whose hdr/meta are replaced.

## Early native observations

The independent initialization answer includes all three complete zero headers,
metadata and seven locals. The 48 operational body profiles vary Ethernet
validity, prior drop, action overlay, dirty fields/locals and three unused TCP
shapes. Overlay hdr shadows a deliberately wrong block hdr, genuinely testing
action-first read. Malformed unused aggregate contents are appropriate examples
of the broad theorem, not purported valid initial source states.

The native comparison uses complete order-independent scope/map helpers plus
whole shared-state comparison; it does not settle for index.program or a few
touched Bloom cells. Short register arrays and an unrelated counter witness
arbitrary preserved state, not actual startup extern binding. Final independent
native/audit/Python checks and adversarial observer review remain pending.

## Stable observer and audit pass

Read the complete new Python body suite and its reused freeze implementation.
Expected initialization constructs all nine roots independently. Body checks
snapshot the actual Env before real stmt.execute, then compare every detached
type-tagged field afterward: dataclasses, maps, protobuf declarations, action
layers, installed entries/index, arbitrary extern storage, packet/emitter and
visits. Unknown object forms fail closed. The 48 profiles parallel the native
semantic dimensions but are not falsely presented as byte-identical cross-
language initial states: native counter/Python register sentinels differ.

Eight real delegating statement faults target a local, unused TCP, Bloom cell,
index, scope, installed entries, cursor int-to-float and action-overlay value.
Each must hit once and fail the complete Env assertion. A separate actual
local-write fault survives packet/extern DRT on an invalid-IPv4 packet, then
the same scoped fault is rejected by full Env observation; exact hit counters
exclude an unexercised control. This cleanly distinguishes the body property
from weaker externally visible packet observations. No observer repair or
expected value is derived from the mutated runtime result.

Independent focused Python execution: 58 passed, exit 0 (1.70 seconds).
Fresh pinned Lean query checks all thirteen default roots: exactly the standard
three axioms. The same query executes the native test module: all nine initial
roots and 48 complete invalid-body profiles pass, exit 0. Public/test/audit
registrations match those advertised roots. No candidate rebuild was performed.
No new finding identified; isolated source-fault proof rejections, final
restoration and durable evidence remain outstanding before final clearance.

## Final source campaigns and restoration

Read the completed durable note and actual source/proof logs. Each of the two
outer guards was independently changed to literal true in the isolated authored
source. The actual source module compiles, then the unchanged body proof fails
at the corresponding real conditional work-queue equation. These are genuine
application-source proof rejections, not runtime disagreement or a linter/setup
failure. They preserve the distinction from a fault in generic IR semantics.

The actual Python conditional implementation was separately edited to write
reg_pos_two=17 on the false first firewall branch, with the required Bits import.
It compiles and returns normally. Packet/extern DRT still agrees, while both
plain and action-overlay whole-Env observations reject the hidden local change.
The initial missing-import setup failure is explicitly excluded from evidence.
No generic divergence bundle or new distinct packet witness is invented for
this real packet-observer survivor; tracked internal constructors and the
permanent regression reproduce the stronger observation.

Independently compared six entire source files across candidate/restored tree:
application source, main, proof, native tests, Python body tests and actual stmt
runtime all match exactly; runtime git diff is empty. Independently executed
restored plain and overlay complete-Env checks, both pass. Fresh exports from
both executables still equal the unchanged 13,588-byte source export, SHA-256
`5342abef71eb652082d9bf2cf09ba6059b66d3f135ee7742c23fb58380722ecd`.

Final owner-attributed gates, corroborated by logs: both packages/default audits/
native suites pass (540 spec checks plus nine initialized roots and 48 body
profiles); focused 58 pass; required 1,476 pass/1,701 deselected with no skips;
complete Python/schema/static/workflow gate 3,171 pass, five existing strict
expected discrepancies and one explicit unavailable optional local XDP-image
skip. Independent fresh thirteen-root queries/native run, focused 58 and
restoration checks are separately recorded above; the reviewer did not rerun
the full expensive suite. No code changed while these consumers ran.

No outstanding finding remains within this exact initialization/body boundary.
The two proposed logical commits may separate proof/public/default audits from
native/Python/evidence. No positive Bloom, parser, whole-call, whole-pipeline or
universal Python correctness result follows from this clearance.
