# Real forwarder ingress prefix

2026-09-23. Scoped implementation plan at committed `9a12253`, checkpoint3
of [the selected-table plan](forwarder-table-next.md). This file records the
current plan; completed proof/test/campaign evidence will be added separately.

## Paused WIP checkpoint

Implementation is paused at the user's definition-of-done discussion, not
abandoned. No code is committed or pushed; all changes remain in the isolated
`work/forwarder-ingress` worktree, based on `9a12253`. No active builds or
binary consumers remain. Do not mistake this WIP note for final acceptance.

- Core `ForwarderIngress` compiles at default limits. It includes exact body
  decomposition, the separately spelled checksum syntax, actual guard law,
  raw header-only invalid prefix, typed corollary, valid actual-application
  composition, conditional concrete readiness, complete source/Run result
  and actual initialized witness. The raw theorem imposes no metadata,
  installed-entry, scope, action-absence or stored-field-validity premises.
- `scripts/check-lean.sh` exited0: both packages, all default audits and
  native tests. New tests pass 540 prefixes, 1620 exact queue boundaries and
  12 raw invalid controls, including missing metadata, malformed unused
  contents and an action hdr shadowing a valid block decoy. Eleven new
  audits have their exact expected foundations (body_identity `[propext]`,
  the other ten the standard three). Logs:
  `/tmp/p4blo-forwarder-ingress-core.log`, `...-native.log`, `...-lean.log`.
- Independent plan review is CLEAR (`reviews/forwarder-ingress-plan.md`).
  Early core structural review also found no remaining issue after adding
  the raw header-only theorem. Final implementation review is **not done**.
- `test_lean_forwarder_ingress.py` is a draft: 540 primary cases, six direct
  normal-return anchors, four raw invalid profiles, ten malformed-export
  and seven observer faults, plus default-target guard. It has **not been
  executed** against the exporter. Ruff format/check pass; latest Pyright
  exits1 at line484 because the drafted installed-state mutation accesses
  nonexistent `InstalledEntries.defaults`. The attribute must be corrected
  and expectations/observers executed before any passing Python claim.
  Logs: `/tmp/p4blo-forwarder-ingress-ruff.log`, `...-pyright.log`.
- No new isolated source-mutant campaign, packet bundle, prior-export byte
  comparison, required DRT gate or full Python gate has run for this slice.

If this bounded slice is retained in the revised finish line, resume by
fixing the one draft static error and running/debugging the focused suite;
then perform the scoped real-source challenges with honest state-only vs
packet distinctions, restored comparisons, required/full gates and final
independent review before small coauthored commits. Do not begin checksum
or further pipeline work merely because this prefix exists.

## Exact boundary

Keep `Forwarder.program` and both original conditionals unchanged. Prove a
literal decomposition of `Forwarder.ingress.body` into its IPv4-only table
conditional and its full checksum conditional. The latter includes the real
144-bit concatenation and destination lvalue, not merely a same-named extern.

Start at `.statements Forwarder.ingress.body :: K`. Stop at
`.statements [checksumConditional] :: K`, including the empty selected-branch
list pop. The checksum guard itself has not run, even after default drop.
Ethernet validity, prior drop and TTL do not gate the first conditional.
The independent policy is: if stored IPv4 validity is true, use the committed
independent table-application policy; otherwise preserve the complete store.
The exact operational result similarly uses the existing ordered-map Run
result only on the valid branch, and is the original Run otherwise.

Provide a general invalid-prefix theorem requiring actual index and source
header value agreement, but **no installed entries, actual control scope or
action-free-frame premise**. It only reads the actual IPv4 validity. Valid
composition retains the application theorem's concrete scope, BlockFrame,
real installed configuration and both index identities. A combined theorem
may expose these concrete readiness facts conditionally on valid IPv4; this
is a state-admissibility premise, never a callback assuming branch correctness.
Supply a separate actual-built initialized-frame witness for arbitrary source
stores/configurations. Preserve full non-frame state and unrelated block vars.

Use the existing HeaderRef read bridge, actual Execution dispatch/step and
ForwarderApply.source_steps. No new evaluator, AST, fuel or runtime change.
The expected explicit trace lengths are 18/12/10 for valid forward/drop/
NoAction and3 for invalid IPv4. Verify these against actual queues rather
than claiming the unindexed Steps relation carries a numeric theorem.

## Independent acceptance

Native and Python profiles will reuse the exact committed configuration/
address inventory and independent raw state expectations, supplementing it
with explicit validity-pair/TTL/prior-drop coverage and invalid entries-none
or adversarial installed state. Observe the entire Run/Env, including complete
index, scope, installed maps, packet/emitter/externs/visits and action layers.
Anchor the full two-condition body independently, including concat order and
pending checksum destination. Check the one-short branch-pop boundary and
arbitrary suffix identity; prove that a next step really begins the checksum
conditional rather than claiming K can run before that pending work.

Python must execute the actual first conditional selected from the actual
full source, with a second observation through real whole-body execution that
stops **before** checksum dispatch. A scoped sentinel, if needed for this
prefix-only observer, is instrumentation and not successful whole-body return.
Count exactly one validity read and, only on valid IPv4, one real application/
key/lookup/action. Freeze complete independently expected state before execution
and immediately after the guard read, preventing repaired side effects from
escaping. An invalid branch must not request entries or run checksum/table.

Challenge compiling actual Lean guard/queue faults against proofs; a valid
but unintended fixture against independent literals; and an actual Python
guard fault against the complete prefix observer. Save/replay a genuine packet
mismatch only if the fault actually changes packet behavior. State-only faults
must be labeled as such, never converted into invented packet witnesses.
Recursively deduplicate complete retained requests against main artifacts.
Preserve every prior exporter byte and runtime/golden identity. Build before
consumers; independent read-only review precedes small coauthored commits.

Confidence: high in the concrete semantic boundary and reuse; medium in the
conditional-readiness API and compact finite profile inventory. Revisit the
API when checksum completion needs the same admissibility facts, and profile
compression if an intentional wrong-guard/side-effect fault survives. No new
checksum, parser, whole ingress/pipeline, global validity or arbitrary routing
claim is part of this checkpoint.
