# Semantic mutation campaign, 2026-09-23

Base: `86b3878` in the isolated `verification/mutation-campaign` worktree.
Each row was applied alone to the actual Python or Lean semantics, then
reversed with an anchored patch. No test or gate code was changed. The
mutations below are recipes, not committed source changes.

## Gate and classification

The pinned baseline ran from the worktree root:

```sh
nix develop -c sh -c 'cd /Users/qobilidop/my/work/p4blo-mutation-campaign/lean && lake build'
nix develop -c sh -c 'P4BLO_REQUIRE_LEAN=1 uv run pytest tests/test_drt.py -k lean_agrees -q'
```

The build exited 0; DRT exited 0 with `11 passed, 38 deselected`.
For every mutant, the second command above was run exactly. For Lean
mutants, `lake build -q` was run first in the same pinned shell and
exited 0. Thus failures below are runtime semantic disagreements, not
compiler failures. The gate compares 200 generated requests per corpus
program with seed 42, including abstract extern state.

| ID | Anchored semantic patch (`old` → `new`) | Gate result |
|---|---|---|
| P1 | `python/p4blo/externs/counter.py`, `self.counts[index.value] += 1` → `+= 0` | Killed: exit 1, `stateful` 200/200 diverged. Displayed packet outputs match; observed counter state differs. |
| P2 | `python/p4blo/externs/register.py`, in `case "write"`, `self.cells[index.value] = value` → `= Bits(self.width, 0)` | Killed: exit 1, `stateful` 200/200 and `register_bounds` 64/200 diverged. The latter had both packet and state mismatches. |
| P3 | `python/p4blo/externs/register.py`, in `case "read"`, `else Bits(self.width, 0)` → `else Bits(self.width, 1)` | Killed: exit 1, `register_bounds` 170/200 diverged, including packet bytes (`903b01...` against `903b00...`). |
| L1 | `lean/P4blo/Externs.lean`, in `.counter counts, "count"`, `counts[index.value] + 1` → `+ 0` | Built; killed: exit 1, `stateful` 200/200 diverged. Displayed packet outputs match; observed counter state differs. |
| P4 | `python/p4blo/interp/expr.py`, in `BINARY_OP_ADD_SAT`, `Bits(n, min(x.value + y.value, (1 << n) - 1))` → `Bits.wrap(n, x.value + y.value)` | Survived: exit 0, `11 passed, 38 deselected`. A sum that exceeds the width now wraps instead of saturating. |
| L2 | `lean/P4blo/Eval.lean`, in `.shr`, `else Bits.wrap n 0` → `else x` | Built; survived: exit 0, `11 passed, 38 deselected`. Shifts by the width or more now retain the operand instead of yielding zero. |

P1 and L1 demonstrate that state comparison detects a counter fault
invisible in packets. P4 and L2 show that the fixed corpus does not
exercise these scalar edge cases through the required DRT gate. The
next iteration should generate well-typed expressions that force an
overflowing saturating add and a right shift amount at least the operand
width, expose their results in output or control flow, then rerun the
original and both mutants. This is a selected campaign, not a measure
of overall mutation coverage.

After the last restoration, the pinned Lean build and required DRT
gate again exited 0 (`11 passed, 38 deselected`); `git diff` showed no
remaining interpreter changes.

## Generated-program follow-up on `14b7a80`

The new `tests/test_drt_programs.py` puts generated scalar expressions
in an observable output field. From the worktree root, the clean
baseline `P4BLO_REQUIRE_LEAN=1 nix develop -c uv run pytest
tests/test_drt_programs.py -q` exited 0 (`12 passed`). Lean builds used
`nix develop /Users/qobilidop/my/work/p4blo-mutation-campaign -c lake
build -q` with `workdir` set to the absolute `lean` directory; each
mutant build exited 0. Each mutant then ran the generated suite with
`P4BLO_DRT_FAILURE_DIR=/tmp/p4blo-mutation-campaign-20260923-<id>
P4BLO_REQUIRE_LEAN=1 nix develop -c uv run pytest
tests/test_drt_programs.py -q --tb=short`. The saved bundles are
temporary evidence, not committed artifacts.

| ID | Anchored semantic patch (`old` → `new`) | Generated-suite result |
|---|---|---|
| P4 again | Same Python `BINARY_OP_ADD_SAT` patch above: saturate → wrap | Killed: exit 1, 8 failed, 4 passed. Seven `test_lean_agrees_on_every_scalar_operator` widths and the shrinking property failed. |
| L2 again | Same Lean `.shr` patch above: oversized shift yields zero → `x` | Built; killed: exit 1, 8 failed, 4 passed, with the same seven systematic widths and shrinking property. |
| P5 | `python/p4blo/interp/expr.py`, boolean cast `expect_bits(operand).value == 1` → `== 0` | Killed: exit 1, 1 failed, 11 passed. Only the shrinking property caught it; the systematic operator loop has no cast case. |
| L3 | `lean/P4blo/Eval.lean`, `.concat` value `(x.value <<< y.width) ||| y.value` → `(y.value <<< x.width) ||| x.value` | Built; killed: exit 1, 8 failed, 4 passed, including all systematic widths and shrinking. |

P4's shrinking property was also run alone with `-k shrinking -q
--tb=short` (exit 1, one failure, 11 deselected). Its saved minimal
reported draw was `bit<7>(1) |+| bit<7>(127)`, on an empty packet:
mutant Python emitted `00`, Lean emitted `fe`. The bundle was
`/tmp/p4blo-mutation-campaign-20260923-p4/scalar-50ed3649e7376630.json`.

L2's shrinking property was run alone with the same options (exit 1,
one failure, 11 deselected). Its saved reported draw was
`~mux(false, bit<1>(0), bit<1>(1) >> bit<1>(1))`: Python emitted `80`,
mutant Lean emitted `00`. The shift itself has the simpler systematic
witness `bit<1>(1) >> bit<16>(1)` (Python `00`, mutant Lean `80`). The
shrinking bundle was
`/tmp/p4blo-mutation-campaign-20260923-l2/scalar-a76f26b8d8aae361.json`.
These are Hypothesis's reported shrunk draws, not claims of globally
smallest semantic witnesses.

P5's reported shrunk witness was `(bool) bit<1>(0)` (mutant Python
`80`, Lean `00`), saved as
`/tmp/p4blo-mutation-campaign-20260923-p5/scalar-b7e77df21d4a1484.json`.
L3's reported shrunk draw was `~~(bit<1>(0) ++ bit<6>(1))`
(Python `02`, mutant Lean `04`), saved as
`/tmp/p4blo-mutation-campaign-20260923-l3/scalar-153180ee689348bd.json`.

After restoring both former survivors and rebuilding clean Lean, each
shrinking bundle was replayed with `P4BLO_REQUIRE_LEAN=1 nix develop
-c uv run python -m p4blo.drt.replay <bundle>`: both exited 0 with
`1 agreed, 0 diverged`. After all four mutations were restored, the
clean Lean build exited 0 and the generated suite again passed all 12
tests. No interpreter mutations or replay artifacts are committed.

## Evaluation-order follow-up on `f04230d`

`tests/test_drt_programs.py` now has a parser-condition case whose
unselected operand is `(bool) lookahead<bit<1>>()` on an empty packet.
Evaluating that operand faults; respecting selection emits packet
`80`. This challenges control flow that pure closed expressions
cannot distinguish. Baseline commands from the worktree root:

```sh
P4BLO_REQUIRE_LEAN=1 nix develop -c uv run pytest tests/test_drt_programs.py -k 'not faulting_unselected' -q
P4BLO_REQUIRE_LEAN=1 nix develop -c uv run pytest tests/test_drt_programs.py -k faulting_unselected -q
```

Both exited 0: 13 passed/1 deselected and 1 passed/13 deselected,
respectively. Each patch below was applied alone to
`python/p4blo/interp/expr.py`, then the first command and the focused
command with `--tb=short` were run. The older 13 tests passed each
time; the focused test failed each time (exit 1, one failed and 13
deselected). No Lean rebuild was needed for these Python-only faults.

| ID | Anchored patch | Focused failing case and result |
|---|---|---|
| P6 | `BINARY_OP_AND`: `expect_bool(evaluate(b.left, env)) and expect_bool(evaluate(b.right, env))` → the same operands joined with `&` | `false && trap`: actual packet `00`, expected `80`. |
| P7 | `BINARY_OP_OR`: `expect_bool(evaluate(b.left, env)) or expect_bool(evaluate(b.right, env))` → the same operands joined with `\|` | `true \|\| trap`: actual packet `00`, expected `80`. |
| P8 | `mux`: replace the conditional evaluation of the chosen branch with `condition = expect_bool(evaluate(m.condition, env)); then = evaluate(m.then, env); otherwise = evaluate(m.otherwise, env); return then if condition else otherwise` | `mux(true, true, trap)`: actual packet `00`, expected `80`. |

The focused test's direct Python output assertion failed before its
Lean comparison, so these are semantic kills by that assertion rather
than recorded Python-versus-Lean divergences. The test contains both
selection directions and selected-branch faults, though each mutant
run stops at its first failure. After restoring every patch, the full
required generated suite exited 0 with `14 passed`, and `git diff`
showed no interpreter changes. No mutation is committed.
