# Closure and cleanup independent review

Read-only review in `/private/tmp/p4blo-closure-review`, separate from the
implementation authors. Compaction reviewed at
`9bd543428cb3eff78cec2893c79f5d91f4cd3677` against archive
`9fc6c19febf839fa56873be10788b515c4e29ae9`; README/design corrections against
`fffbac7d54782f26f6dd2a7464d05dad55559f16`. Runner cleanup reviewed at exact
`a31ff1951034221e86bd2a27e16b3b72873f2f6b` against `9bd5434`.
This is independent AI-agent review, not human approval.

## Findings and disposition

Two draft status findings were fixed before `9bd5434`: family registration
was incorrectly described as per-instance (binding factories create that
state); an enumerated progress-premise summary omitted successful installation
and typed parameter values. Final status separates registration/binding and
refers to the full execution premises in assurance. No unresolved confirmed
defects remain.

Read archived/current decisions in full: current choices, reasons, original
dates and revisit triggers survive. Status preserves finite-scope results,
qualified claims, exact evidence revisions, the invalidated-run distinction,
semantic/proof/architecture gaps and nonblocking observations. Roadmap and
parked notes are unchanged. Completed plans/reflection/reviews are historical
and recoverable at the archive; their artifact contracts already live in docs.
Mutation scripts remain because workflows cites them. README/design corrections
remove the obsolete source-execution exclusion and normative language without
claiming general compilation or universal equivalence.

The runner fix catches only `BrokenPipeError` from buffered stdin close after
process cleanup and worker joining. It retains kill-before-close and live-worker
protections and permits stdout/stderr cleanup. The real-pipe regression waits
for peer exit and checks ProtocolError with exit 3, closed streams, stopped
worker, cleared resources and repeatable close.

## Independent checks

- `git diff --check 9fc6c19..9bd5434`: exit 0. Review worktree clean.
- Every repository evidence SHA in status resolves through
  `git cat-file -e <sha>^{commit}`; upstream P4-SpecTec pins are not local
  repository commits.
- Docs links: 2 passed with the main checkout's existing Python interpreter,
  `PYTHONPATH` explicitly selecting the review checkout's implementation,
  `PYTHONDONTWRITEBYTECODE=1` and pytest `-p no:cacheprovider`.
- The same environment, `pytest tests/drt/test_drt_protocol.py
  tests/drt/test_drt.py -q -p no:cacheprovider -k 'not lean_agrees'`:
  75 passed, 14 deselected, 8.31 s.
- Extracted exact archived `LeanRunner.close` at `9bd5434` through AST and
  recompiled it into the test process only, then ran
  `test_dead_peer_buffered_write_preserves_error_and_closes_streams`.
  It failed with `BrokenPipeError` at archived close line 380 instead of
  ProtocolError. The harness asserted pytest TESTS_FAILED; no source was edited.

## Integrator checks

The full `P4BLO_REQUIRE_LEAN=1 scripts/check.sh` on `a31ff19` exited 0:
5200 passed, one optional local XDP-image skip, four expected failures;
lint, types, schema, generation and workflow checks passed. The author ran
134 focused protocol/state/replay tests, with 15 Lean cases deselected, and
Ruff/Pyright. Fresh three-package Lean checks earlier in closure passed;
this repair changes no Lean or core-semantic source.

## Limits

No independent full gate, Lean rebuild, external oracle/mutation campaign,
cache timing experiment or remote CI run for closure. Author/integrator checks
remain separately attributed. Final PR head must have successful applicable
remote CI before merge; skips are not passes and earlier gates are not
final-head evidence.
